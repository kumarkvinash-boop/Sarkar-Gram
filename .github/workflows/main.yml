name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Buildozer Action
        uses: akshay811/buildozer-action@v1.1
        with:
          buildozer_version: stable
          python_version: 3.10
